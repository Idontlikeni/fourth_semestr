def naive_mul(x, y):
    s = 0
    for i in range(y):
        s += x
    return s


a = int(input())
b = int(input())

assert naive_mul(a, b) == a*b, 'Неверно'
assert naive_mul(10, 15) == 10*15, 'Неверно'